feat(ui): hyperparameter panel + tab layout + job submission (Phase 8b)

Backend:
- config.py: TrainConfig gets motion regularizer fields
  (lambda_deform_reg, lambda_smoothness, lambda_rigidity)
- pipeline.py: pass motion reg values to Trainer
- api.py: /process accepts 18 optional hyperparameter overrides:
  training (iters/resolution/num_timestamps/fps), loss weights,
  learning rates, density control, model architecture

Frontend (Phase 8b):
- api.ts: submitJob(File, opts) with FormData upload, full types
- HyperparameterPanel: 5 collapsible groups, override count badges,
  null preserves backend default
- JobSubmitPanel: HTML file picker (no Tauri plugin needed), auto
  scene name from filename, preset chips, skip_foundation toggle,
  integrated hyperparameter panel
- JobsList: polls /jobs every 2s, progress bars, expand with
  traceback + "Viewer'da ac" + ".zip indir" actions
- App.tsx: 3-tab navigation (Yeni Job / Jobs / Viewer), viewer
  moved to own tab with key={job_id} remount guard
- App.css: 200+ new lines for tabs, hparam groups, job rows,
  progress bars, preset chips, file picker

CSS sizing fix: viewer-canvas gets flex:1 + min-height:300px
to prevent collapse when grid math fails.

No new runtime dependencies. Vite/React/Three.js stack unchanged.
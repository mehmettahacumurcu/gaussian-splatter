feat(stage2): foundation losses + bigger MLP + pipeline hardening

Backend — Stage 2 motion priors:
- trainer.py: depth consistency loss (scale-invariant L1 via
  median ratio alignment) + mask-weighted reconstruction
  (dynamic pixels get 1 + lambda_mask × mask weight)
- deformation.py: MLP width 256→512, depth 2→4 hidden layers,
  Fourier time encoding (num_time_freqs=6, 2L+1 dims appended
  to HexPlane features). ~3.6× parameter count vs v1, still
  trivial compared to N×300K gaussians.
- config.py: new TrainConfig fields (lambda_deform_reg=1e-3,
  lambda_smoothness=1e-2, lambda_rigidity=1e-2, lambda_depth=0.1,
  lambda_mask_motion=1.0) + ModelConfig (mlp_depth, num_time_freqs)
- renderer.py: with_depth=True now runs TWO separate gsplat
  rasterization calls (RGB + ED) instead of RGB+D mode —
  works around version-specific background shape quirks.

Backend — robustness:
- pipeline.py: foundation phase now resilient (try/except per
  subphase, one failure doesn't abort pipeline). COLMAP cache
  check: if parse_cameras succeeds on existing data/<scene>/colmap,
  skip rerun. Threads depth_dir/mask_dir to trainer when
  foundation ran.
- run_colmap.py: subprocess.Popen with line streaming, regex
  parsing of feature_extractor / matcher / mapper stdout, calls
  on_progress(fraction, message) with phase-mapped ranges
  (feat 0-0.15, match 0.15-0.40, mapper 0.40-0.95, converter
  0.95-1.0). End of 6% progress freezes.

Backend — Metric3D → MiDaS:
- depth_estimate.py rewritten: intel-isl/MiDaS via torch.hub
  (no mmcv/mmengine dependency which was Windows install hell).
  Supports DPT_Small/Hybrid/Large. Old metric3d_vit_* names
  auto-map. Outputs are inverse-depth → inverted to depth for
  intuitive scale-invariant comparison.

Backend — API surface:
- api.py: /process accepts 24 optional hyperparameter overrides
  grouped (training, losses, lr, density, model, foundation).
  All default None → backend preset/default used if unset.

Frontend (Phase 8b continuation):
- HyperparameterPanel: 6 collapsible groups with override badges,
  Foundation models group added, lambda_depth + lambda_mask_motion
  + mlp_depth + num_time_freqs fields wired to API.
- JobSubmitPanel: skip_foundation default changed to False (now
  that foundation actually runs and integrates with trainer).
- api.ts: HyperParams interface expanded with all new fields.

Scripts:
- hypernerf_to_mp4.py: switched from -vsync vfr to -fps_mode cfr
  with -r <fps>, prevents VFR output that confused downstream
  ffmpeg fps filter (was extracting 3× expected frame count).

Verified via smoke tests:
- Foundation phase end-to-end (MiDaS depth + CoTracker tracks +
  Farneback masks all produce expected .npy/.pt/.png outputs).
- Training completes with depth + mask losses active (log shows
  "Depth loss aktif" / "Mask-weighted recon aktif").
- COLMAP progress hook streams %-progress per phase.

Motion still subtle — next session will connect CoTracker tracks
into trainer (currently generated but not consumed) for stronger
motion supervision signal. Task #41 RAFT dense flow still deferred.